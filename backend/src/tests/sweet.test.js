describe("Sweet Inventory Logic", () => {
  it("should reduce quantity on purchase", async () => {
    expect(true).toBe(true);
  });

  it("should prevent purchase if out of stock", async () => {
    expect(true).toBe(true);
  });
});
